(function exe(canv){
  const cx = canv.getContext("2d");
  const inputs = document.querySelector("input");
  const m = inputs[0];
  const x = inputs[1];
  const b = inputs[2];

  /*
    Todo:
    1. Generate equation or value for given params
    2. Generate graph for given prarams
  */
})(document.querySelector("canvas"));
